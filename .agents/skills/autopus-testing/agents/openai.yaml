interface:
  display_name: "Autopus Testing"
  short_description: "Choose cheap, targeted Autopus validation"
  default_prompt: "Use $autopus-testing to choose the cheapest safe test or CI verification path, inspect failures, and rerun only the relevant Autopus lane."
