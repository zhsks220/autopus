---
summary: "CLI reference for `autopus clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `autopus clawbot ...`
  - You need migration guidance to current commands
title: "Clawbot"
---

# `autopus clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `autopus clawbot qr` (same behavior as [`autopus qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `autopus clawbot qr` -> `autopus qr`

## Related

- [CLI reference](/cli)
